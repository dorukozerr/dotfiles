#!/usr/bin/env bash

# Tmux Timer Toggle

if tt_invoke is_running; then
  # Timer is running - open log popup
  tt_log
else
  # No timer - start it
  tt_invoke start
  tmux display-message "Timer started!"
fi
